'use client';
import React, { useState } from 'react';

const SideNavbar = () => {
  const [isNavOpen, setIsNavOpen] = useState<boolean>(false);
  return <div className="w-[250px]">SideNavbar</div>;
};

export default SideNavbar;
